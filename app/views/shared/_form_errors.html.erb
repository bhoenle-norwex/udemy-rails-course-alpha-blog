<% if entity.errors.any? %>
    <h2>The following errors prevented the <%= entity.class.name %> from being saved:</h2>
    <ul>
        <% entity.errors.full_messages.each do | msg | %>
            <li><%= msg %></li>
        <% end %>
    </ul>
<% end %>
